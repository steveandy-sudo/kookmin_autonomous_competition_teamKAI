# 260623_1502 Commit Notes

## Package Hierarchy

```text
xycar_ws/
├── ROS-TCP-Endpoint-main-ros2/      -> ros_tcp_endpoint
└── src/                             -> track_drive repository root
    ├── track_drive/                 -> main autonomous driving, perception, safety, and debug nodes
    ├── cone_il/                     -> cone imitation-learning tools and AI driver source
    ├── launch/                      -> integrated and debug ROS2 launch files
    ├── assets/models/               -> runtime TorchScript and ONNX model assets
    ├── rviz/                        -> RViz debug layouts
    ├── docs/                        -> training and architecture notes
    ├── package.xml
    └── setup.py
```

`colcon list` discovers:

```text
ros_tcp_endpoint    ROS-TCP-Endpoint-main-ros2
track_drive         src
```

`cone_il/` remains a package-like support tree under this repository layout.

## Runtime State

- Default cone AI model remains `assets/models/cone_bc_scripted_5.pt`.
- Left-turn execution is disabled by default with `intersection_left_turn_enabled=false`.
- School-zone speed limit is currently `17.5`.
- The camera QoS depth remains `1` for lower-latency image handling.

## Source Changes

### `launch/ai_direct_hybrid.launch.py`

- Disabled left-turn execution by default again:
  - `intersection_left_turn_enabled=false`

### `track_drive/intersection_decider.py`

- Added Korean comments explaining the wrapper role of each function.
- The file now clearly points readers to `TrackDriverNode` for the actual route decision logic.

### `track_drive/*.py`

- Added Korean function-block comments throughout the `track_drive` package.
- Each function now has a short explanation of what logic it owns.
- Covered driving control, LiDAR utilities, perception, safety supervision, stop-line detection, school-zone detection, traffic-light detection, debug nodes, and the main `track_drive.py` node.

### `track_drive/track_drive.py`

- Added Korean comments to the main driving, safety, perception, and intersection helper functions.
- Preserved the existing runtime behavior except for the requested left-turn default switch.

## Validation

```bash
python3 -m compileall -q track_drive
```

Additional checks:

```bash
ros2 launch track_drive ai_direct_hybrid.launch.py --show-args
```

Confirmed:

```text
intersection_left_turn_enabled=false
school_zone_speed=17.5
```
