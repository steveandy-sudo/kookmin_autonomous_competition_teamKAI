# 2026-08-19 KTY 첫 수정

## 오늘 수정한 내용

1. 신호등 판단
   - `red_4`, `yellow_4`, `green_4`의 bbox 크기를 거리 조건으로 사용하지 않도록 변경했습니다.
   - confidence와 기존 2프레임 확인 조건은 유지했습니다.

2. 차량 및 장애물 회피
   - 직선뿐 아니라 곡선에서도 최신 노란 중앙선을 기준으로 장애물의 좌우 위치를 판단하도록 변경했습니다.
   - 장애물이 차량 오른쪽에 있으면 왼쪽으로, 왼쪽에 있으면 오른쪽으로 회피합니다.

3. 라바콘 주행 복귀
   - YOLO와 LiDAR에서 콘이 모두 사라진 뒤 센서 유효시간 `0.5초`가 지나면 RULE 주행으로 복귀하도록 변경했습니다.
   - 기존의 추가 복귀 대기시간 `1.0초`는 제거했습니다.

## 2026-08-19 KTY 두 번째 수정

1. VESC 가속 및 저전압 복구
   - 가속 제한을 `0.6m/s²`에서 `0.4m/s²`로 낮춰 출발 전류가 더 천천히 증가하도록 변경했습니다.
   - speed command `0 → 10` 상승 시간은 약 `2.0초`이며 감속 제한 `1.5m/s²`는 유지했습니다.
   - 저전압 fault 해제 후 자동 복구 안정 시간을 `3.0초`에서 `1.0초`로 줄였습니다.
   - 복구 전압 `8.0V`, 출력 제한 시작 `7.5V`, 출력 차단 `6.0V` 기준은 유지했습니다.

2. 통합 주행 터미널 상태 표시
   - RULE, 지름길, 라바콘, 장애물 회피, 신호등처럼 실제 주행 모드가 바뀔 때만 상태를 표시하도록 정리했습니다.
   - 정지할 때는 Space 정지, 후보 명령 단절, 주행 선택기 정지, VESC 저전압 및 fault 등 확인 가능한 원인을 항상 함께 표시합니다.
   - 반복되는 객체 검출과 경로 상태 로그는 터미널에서 줄이고 전체 launch 로그에는 그대로 보존합니다.

3. 실차 주행 rosbag
   - 기록 이름: `8_19_first_drive`
   - 현재 로컬 폴더: `/home/xytron/rosbags/integrated_drive/8_19_first_drive_지름길_신호등_곡선_콘_장애물회피`
   - 기록 시각: `2026-08-19 10:42:21~10:47:17 KST`
   - 길이: `296.2초`, 메시지 `110,925개`, Zstd 압축 크기 `1.6GiB`
   - 주요 평균 주기: 압축 카메라 `29.74Hz`, Canonical `9.48Hz`, LiDAR `9.63Hz`, VESC `49.98Hz`, 최종 모터 명령 `20.06Hz`
   - 카메라·인지·LiDAR·RULE 후보·콘·회피·최종 조향과 속도·VESC 전압 및 diagnostics가 포함되어 있습니다.
   - 대용량 rosbag 원본은 Git 저장소에 올리지 않고 위 실차 PC 경로에 보관합니다.
