# 2026-08-19 KTY 첫 수정

## 오늘 수정한 내용

1. 신호등 판단
   - `red_4`, `yellow_4`, `green_4`의 bbox 크기를 거리 조건으로 사용하지 않도록 변경했습니다.
   - confidence와 기존 2프레임 확인 조건은 유지했습니다.

2. 차량 및 장애물 회피
   - 직선뿐 아니라 곡선에서도 최신 노란 중앙선을 기준으로 장애물의 좌우 위치를 판단하도록 변경했습니다.
   - 장애물이 차량 오른쪽에 있으면 왼쪽으로, 왼쪽에 있으면 오른쪽으로 회피합니다.

3. 라바콘 주행 복귀
   - YOLO와 LiDAR에서 콘이 모두 사라진 뒤 센서 유효시간 `0.5초`가 지나면 RULE 주행으로 복귀하도록 변경했습니다.
   - 기존의 추가 복귀 대기시간 `1.0초`는 제거했습니다.

## 2026-08-19 KTY 두 번째 수정

1. VESC 가속 및 저전압 복구
   - 가속 제한을 `0.6m/s²`에서 `0.4m/s²`로 낮춰 출발 전류가 더 천천히 증가하도록 변경했습니다.
   - speed command `0 → 10` 상승 시간은 약 `2.0초`이며 감속 제한 `1.5m/s²`는 유지했습니다.
   - 저전압 fault 해제 후 자동 복구 안정 시간을 `3.0초`에서 `1.0초`로 줄였습니다.
   - 복구 전압 `8.0V`, 출력 제한 시작 `7.5V`, 출력 차단 `6.0V` 기준은 유지했습니다.

2. 통합 주행 터미널 상태 표시
   - RULE, 지름길, 라바콘, 장애물 회피, 신호등처럼 실제 주행 모드가 바뀔 때만 상태를 표시하도록 정리했습니다.
   - 정지할 때는 Space 정지, 후보 명령 단절, 주행 선택기 정지, VESC 저전압 및 fault 등 확인 가능한 원인을 항상 함께 표시합니다.
   - 반복되는 객체 검출과 경로 상태 로그는 터미널에서 줄이고 전체 launch 로그에는 그대로 보존합니다.

3. 실차 주행 rosbag
   - 기록 이름: `8_19_first_drive`
   - 현재 로컬 폴더: `/home/xytron/rosbags/integrated_drive/8_19_first_drive_지름길_신호등_곡선_콘_장애물회피`
   - 기록 시각: `2026-08-19 10:42:21~10:47:17 KST`
   - 길이: `296.2초`, 메시지 `110,925개`, Zstd 압축 크기 `1.6GiB`
   - 주요 평균 주기: 압축 카메라 `29.74Hz`, Canonical `9.48Hz`, LiDAR `9.63Hz`, VESC `49.98Hz`, 최종 모터 명령 `20.06Hz`
   - 카메라·인지·LiDAR·RULE 후보·콘·회피·최종 조향과 속도·VESC 전압 및 diagnostics가 포함되어 있습니다.
   - 대용량 rosbag 원본은 Git 저장소에 올리지 않고 위 실차 PC 경로에 보관합니다.

## 2026-08-19 KTY 세 번째 수정

1. 곡선 주행 안정화
   - 통합 주행의 곡선 기본 속도를 `14`에서 `12`로 낮췄습니다.
   - 직선 속도는 그대로 유지하고 곡선으로 확정된 구간에서만 감속하도록 했습니다.
   - 큰 곡선 조향 명령에는 필요할 때만 배수를 시험할 수 있도록 기존 실행 옵션을 유지했습니다.

2. 라바콘 접근과 진입 안정화
   - 콘 YOLO 진입 신뢰도 기준을 `0.50`에서 `0.40`으로 완화했습니다.
   - 콘이 한 개 이상 보이는 순간 기존 RULE 조향은 유지하면서 속도를 최대 `15`로 제한하는 `CONE_APPROACH` 단계를 추가했습니다.
   - 콘이 두 개 이상 보이고 유효한 `cone_cmd`가 들어오면 기존 `CONE_RULE`로 전환해 속도 `8`과 라바콘 경로 조향을 사용합니다.
   - 단일 콘을 차량 회피 대상으로 처리하는 기능은 비활성화해 라바콘 진입과 차량 회피가 서로 꼬이지 않게 했습니다.

3. 차량 회피 응답 조정
   - 회피 횡이동 변화율 기본값을 `0.50m/s`에서 `0.65m/s`로 높였습니다.
   - 회피 속도 상한은 `8`에서 `17`로 높였으며, 실제 최종 속도는 통합 주행 상한과 안전 조건의 제한을 받습니다.

4. 세 번째 실차 주행 결과
   - rosbag 이름은 `8_19_second_drive`이지만 실제 시험 순서상 **3번째 주행**입니다.
   - 현재 로컬 폴더: `/home/xytron/rosbags/integrated_drive/8_19_second_drive`
   - 기록 시각: `2026-08-19 13:09:35~13:15:04 KST`
   - 길이: `329.4초`, 메시지 `123,277개`, Zstd 압축 크기 `1.9GiB`
   - 라바콘 진입이 이전보다 안정됐고 곡선 구간의 속도 전환도 안정적으로 동작했습니다.
   - 압축 카메라, Canonical 차선, LiDAR, RULE 후보, 콘 경로와 명령, 회피 상태, 최종 조향·속도 및 VESC 상태가 기록되어 있습니다.
   - 대용량 rosbag 원본은 Git 저장소에 올리지 않고 위 실차 PC 경로에 보관합니다.

5. 남아 있는 문제
   - 지름길 구간에서 회피 대상 차량이 함께 보이면 지름길과 차량 회피 상태가 서로 영향을 주어 제어가 꼬이는 문제가 남아 있습니다.
   - 출발 후 첫 번째 곡선 구간에서 경로·조향 전환이 불안정하게 꼬이는 경우가 있습니다.
   - 지름길 진입 시 좌회전 조향량이 너무 커서 안쪽으로 과도하게 들어가는 문제가 남아 있습니다.

## 2026-08-19 KTY 네 번째 수정

1. 직선·곡선 지연예측 분리
   - 직선 구간의 제어 지연예측을 `0.20초`로 낮춰 이전 조향 때문에 직선 경로가 반대쪽으로 과도하게 이동하는 현상을 줄였습니다.
   - 곡선 구간은 기존 선행 조향을 유지할 수 있도록 지연예측 `0.35초`를 사용합니다.
   - 곡선 상태가 한 번 확정되면 곡선 지연예측을 최소 `0.50초` 동안 유지해 순간적인 직선 판정으로 예측값이 바로 되돌아가지 않게 했습니다.
   - 곡선 속도 판정과 감속 히스테리시스는 기존 로직을 그대로 사용하고 지연예측 선택에만 유지 시간을 적용했습니다.
   - 실행 시 `CONTROL_LATENCY_PREVIEW_SEC`, `CURVE_CONTROL_LATENCY_PREVIEW_SEC`, `CURVE_CONTROL_LATENCY_MINIMUM_HOLD_SEC`로 각각 조정할 수 있습니다.

2. 진단값 추가
   - `/rule_drive/diagnostics`의 `data[42]`에 현재 적용 중인 지연예측 시간을 기록합니다.
   - `data[43]`에는 곡선 지연예측 유지 상태를 `0/1`로 기록합니다.

3. 현재 실차에서 확인된 문제
   - 초록불이 간헐적으로 지름길 트리거로 인식되는 경우가 있어 YOLO 원본 클래스와 미션 전환 조건을 함께 확인해야 합니다.
   - 진입 속도가 충분하지 않을 때 지름길 조향이 차량 이동거리보다 빠르게 진행되어 과도하게 회전하는 문제가 있습니다.
   - `CONE_RULE` 진입은 되지만 생성된 콘 경로와 조향이 실제 라바콘 통로 중심을 제대로 따라가지 못해 충돌하거나 코스를 벗어나는 등 콘 주행을 잘못하는 문제가 있습니다. LiDAR 좌우 콘 경계, cone 경로, cone 후보 조향과 최종 모터 명령을 같은 시점으로 확인해야 합니다.
   - 지연예측을 직선과 곡선으로 나누기 전에는 통과하던 곡선이 변경 후 불안정해진 사례가 있어 변경 전후 rosbag 비교가 필요합니다.
   - 고속 직선에서 곡선으로 진입할 때 곡선 확정과 감속이 늦어 차량이 곡선 바깥쪽으로 이탈하는 문제가 남아 있습니다.

4. 다음 실차 검증
   - 같은 곡선을 동일한 속도로 3회 주행해 곡선 확정 시점, 지연예측 전환 시점, 감속 시작 시점과 최종 조향을 비교합니다.
   - 지름길은 같은 진입 속도를 유지한 상태에서 조향 시작 시점과 차량 이동거리의 관계를 확인합니다.
   - 라바콘은 LiDAR 좌우 경계, 생성된 cone 경로, cone 후보 조향과 최종 모터 명령을 하나의 rosbag으로 기록해 실제 통로 중심과 비교합니다.
