# This repository intentionally publishes only the active workspace,
# deployment archives, and project reports from the Kookmin directory.
/*
!/.gitignore
!/README.md
!/xycar_ws/
!/reports/
!/compressed/

# Keep only the smaller deployment archives in compressed/.
/compressed/*
!/compressed/*.tar.xz
/compressed/xycar_ws.zip
