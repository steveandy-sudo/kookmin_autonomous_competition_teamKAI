# Python dependencies installed with pip.
# ROS2 Humble packages such as rclpy, cv_bridge, sensor_msgs, std_msgs,
# geometry_msgs, launch, and launch_ros are provided by ROS
# and are intentionally excluded from this file.

numpy
opencv-python
onnxruntime
