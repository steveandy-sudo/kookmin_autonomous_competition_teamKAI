# Lane Fallback controller output for the single Final Driver.
# The controller publishes physical steering angle, never /xycar_motor.

builtin_interfaces/Time stamp
float32 steering_angle_deg
bool valid
