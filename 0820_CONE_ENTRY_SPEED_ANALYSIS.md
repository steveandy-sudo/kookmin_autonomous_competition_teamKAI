# 라바콘 진입 감속 시점 및 예상 진입 속도 분석

- 작성일: 2026-08-20
- 워크스페이스: `/home/xytron/xycar_ws`
- 분석 대상: 통합 주행 상태에서 차량을 손으로 밀어 라바콘 진입구간까지 접근한 실험
- 분석 목적: 차선 구간에서 속도 명령 25로 주행하다가 라바콘 모드의 속도 명령 10으로 전환될 때, 실제 라바콘 길 진입 시점의 예상 속도를 계산하고 진입 판단 과정을 확인한다.

## 1. 결론 요약

이번 수동 진입 실험에서는 통합 주행 제어기가 라바콘 경로를 최초로 유효하다고 판단한 뒤 3회의 연속 유효 명령을 확인하고 `CONE_RULE`로 전환했다.

- 라바콘 모드 전환 시점의 가장 가까운 채택 라바콘
  - 전방거리 `x = 1.320 m`
  - 방사거리 `r = 1.397 m`
- 모드 전환 직전 속도 명령: `25`
- 모드 전환 후 속도 명령: `10`
- 현재 VESC 설정상 환산 속도
  - 명령 `25` → `2.00 m/s` (`7.20 km/h`)
  - 명령 `10` → `0.80 m/s` (`2.88 km/h`)
- 설정 감속도 `1.5 m/s²`를 그대로 달성한다고 가정할 때 `25 → 10` 감속에 필요한 값
  - 시간: `0.80 s`
  - 거리: `1.12 m`

따라서 이번 실험에서 관측한 전방거리 `1.320 m`를 기준으로 하면, 소프트웨어와 구동계 지연이 작을 때 차량은 라바콘 진입 직전 또는 약 `0.06~0.10 m` 전에 속도 명령 10에 해당하는 약 `0.8 m/s`에 도달할 가능성이 높다.

다만 여유 거리가 매우 작다. 지연이 커지면 라바콘 입구에서 명령 10보다 빠른 상태일 수 있다.

| 가정한 총 지연 | 입구 도달 예상 속도 | 속도 명령 환산값 | 해석 |
|---:|---:|---:|---|
| 약 `0.05~0.07 s` | 약 `0.80 m/s` | 약 `10` | 정상적인 소프트웨어 지연 범위에서는 목표 속도에 거의 도달 |
| `0.10 s` | `0.80 m/s` | `10.0` | 감속거리가 거의 정확히 맞음 |
| `0.15 s` | 약 `0.97 m/s` | 약 `12.1` | 입구에서 목표보다 다소 빠름 |
| `0.20 s` | 약 `1.11 m/s` | 약 `13.9` | 입구에서 체감상 명령 14 정도의 속도일 수 있음 |

최종 판단은 다음과 같다.

> 현재 로직은 보통의 조건에서는 라바콘 입구까지 속도 10에 가깝게 감속할 가능성이 높지만, 감속 여유가 약 10 cm 이하로 촉박하다. 배터리, 노면, 타이어, 차량 하중, 인식 지연이 불리한 실제 경기장에서는 입구 진입 속도가 명령값 기준 약 `12~14`까지 남을 가능성이 있다.

## 2. 분석한 rosbag

실험 rosbag 경로:

```text
/home/xytron/xycar_ws/cone_entry_monitor_bags/cone_entry_push_20260820_040831
```

bag 파일:

```text
cone_entry_push_20260820_040831_0.db3
```

bag 기본 정보:

| 항목 | 값 |
|---|---:|
| 시작 시각 | 2026-08-20 04:08:39.638001934 |
| 종료 시각 | 2026-08-20 04:09:22.540673699 |
| 기록 시간 | `42.902671765 s` |
| 전체 메시지 수 | `5,655` |
| 파일 크기 | 약 `2.5 MiB` |

기록된 주요 토픽과 메시지 수:

| 토픽 | 메시지 수 |
|---|---:|
| `/hybrid/rule_candidate` | 415 |
| `/hybrid_gate/diagnostics` | 858 |
| `/hybrid_gate/status` | 858 |
| `/hybrid_gate/xycar_motor_shadow` | 858 |
| `/my_rule/cone_processing_enabled` | 988 |
| `/my_rule/cone_clusters` | 189 |
| `/my_rule/cone_fused_clusters` | 189 |
| `/my_rule/cone_lidar_clusters` | 189 |
| `/my_rule/cone_cmd` | 189 |
| `/my_rule/cone_diagnostics` | 189 |
| `/my_rule/cone_status` | 189 |
| `/my_rule/object_detections` | 129 |
| `/scan` | 415 |

이 실험은 차량을 손으로 밀면서 진입 판단을 관찰한 실험이므로 실제 모터 출력 토픽과 VESC 속도 피드백은 포함되지 않았다. `/hybrid_gate/xycar_motor_shadow`는 통합 제어기가 모터에 내보냈을 명령을 보여주는 모의 출력이며, 실제 차량 속도 측정값은 아니다.

## 3. 라바콘 인식 및 진입 타임라인

아래 시간은 bag 시작 시점을 `0 s`로 둔 상대 시간이다. 실제 `CONE_RULE` 전환 시각인 `29.984 s`를 진입 판단 기준 `0 s`로 함께 표시했다.

| 이벤트 | bag 상대 시각 | 진입 판단 대비 | 인식 및 경로 상태 | 출력 상태 |
|---|---:|---:|---|---|
| YOLO 첫 라바콘 검출 | `23.344 s` | `-6.640 s` | 라바콘 1개, confidence `0.531`; 유효 융합 경로 없음 | RULE, 속도 `25` |
| 라바콘 처리 활성화 | `23.345 s` | `-6.639 s` | 첫 YOLO 검출 후 약 `1 ms` | RULE, 속도 `25` |
| 두 번째 조건 충족 YOLO 검출 | `24.056 s` | `-5.928 s` | 라바콘 1개, confidence `0.544`; 원시 후보 최근접 약 `2.067 m`, 융합 경로 없음 | RULE, 속도 `25` |
| 최초 카메라 융합 후보 | `27.347 s` | `-2.637 s` | confidence `0.814`, bbox 3개, 채택 후보 1개, 거리 `2.176 m` | 유효 조향 명령 없음, 속도 `25` |
| 채택 후보 2개 | `28.586 s` | `-1.399 s` | confidence `0.836`, bbox 5개, 최근접 `1.748 m`; `short_single_boundary`로 경로 기각 | 유효 조향 명령 없음, 속도 `25` |
| 첫 유효 라바콘 명령 | `29.725 s` | `-0.259 s` | confidence `0.862`, 채택 후보 3개, 최근접 `1.458 m` | 물리 조향각 `+1.321°`, 속도 목표 `10`; 선택기는 아직 RULE `25` |
| 세 번째 연속 유효 명령 | `29.950 s` | `-0.035 s` | confidence `0.838`, 채택 후보 4개, 최근접 `1.397 m` | 물리 조향각 `+1.250°`, 속도 목표 `10`; 선택기는 아직 RULE `25` |
| 실제 `CONE_RULE` 진입 | `29.984 s` | `0.000 s` | 3회 연속 유효 라바콘 명령 조건 완료 | Xycar 조향 `+3.126`, 속도 `10` |
| `first_left` 단계 진입 | `34.084 s` | `+4.100 s` | confidence `0.896`, 채택 9개, 최근접 `0.361 m` | 물리 조향각 `-5.311°`, 속도 `10` |

bag 종료까지 `CONE_RULE` 상태가 약 `12.893 s` 동안 유지되었다.

화면에서 실험 막바지에 표시된 약 `0.42 m`, 약 `-13°`는 라바콘 모드에 이미 진입한 뒤의 값이다. 라바콘 진입 판단이 시작된 거리로 사용하면 안 된다.

## 4. 실제 진입 판단 시점의 데이터

`CONE_RULE`로 처음 전환된 시점의 가장 가까운 채택 라바콘 좌표는 다음과 같다.

| 순번 | 전방 `x` | 좌우 `y` | 방사거리 `r` |
|---:|---:|---:|---:|
| 1 | `1.320 m` | `-0.458 m` | `1.397 m` |
| 2 | `1.622 m` | `+0.506 m` | `1.699 m` |
| 3 | `1.693 m` | `-0.313 m` | `1.722 m` |
| 4 | `2.069 m` | `-0.213 m` | `2.080 m` |

해당 시점의 상태 문자열에 포함된 주요 값:

```text
source=right_offset
lidar=5
fused=4
plan=4
LR=1/3
path=100
width=0.850m
yolo_age=0.289s
scan=9.5Hz
steer=+1.3->+1.3deg
path_y=-0.023->-0.023m
held=0
phase=approach
pair_width=0.822m
wide=0
release=+0.0/-1.00s/0
reject=none
```

여기서 화면에 표시된 `1.397 m`는 라이다 원점에서 라바콘까지의 방사거리다. 감속에 실제로 쓸 수 있는 진행 방향 거리는 가장 가까운 점의 `x = 1.320 m`로 보는 편이 보수적이고 정확하다.

## 5. 약 1.4 m 전까지 속도 25가 유지된 이유

YOLO는 라바콘 모드 전환보다 `6.640 s` 먼저 라바콘을 검출했고, 최초 카메라 융합 후보도 전환보다 `2.637 s` 먼저 생성됐다. 따라서 이번 결과는 YOLO가 늦게 본 것이 주원인이 아니다.

초기에는 경로를 만들 수 있을 만큼 신뢰할 수 있는 라바콘 경계가 구성되지 않았다.

- 채택 후보 1개일 때는 주행 경로를 확정할 정보가 부족했다.
- 채택 후보 2개일 때는 한쪽 경계가 너무 짧아 `short_single_boundary`로 기각됐다.
- 현재 설정의 `single_boundary_min_control_arc_m = 0.50 m` 조건을 만족하지 못했다.
- 채택된 오른쪽 경계 후보가 3개가 되면서 처음으로 유효 경로가 만들어졌다.
- 이후 3회의 연속 유효 명령을 확인한 뒤 `CONE_RULE`로 전환됐다.

3회 연속 확인으로 추가된 시간은 약 `0.225 s`이며, 이번 수동 이동 속도에서는 약 `0.061 m`에 해당했다. 즉, 약 1.4 m까지 전환되지 않은 핵심 원인은 3프레임 래치 자체보다 초기 경로 형상 부족이었다.

이 조건은 책상이나 의자 일부, 관련 없는 라이다 점군을 라바콘 경계로 잘못 연결하는 것을 막는 안전장치이므로 조향 진입 조건을 단순히 완화하는 것은 위험하다.

## 6. 카메라-라이다 융합의 동작 확인

초기 원시 라이다 후보가 모두 실제 라바콘 경로 후보로 채택된 것은 아니다.

- 첫 YOLO 검출의 bbox: `(770, 401) - (805, 447)`
- 같은 시기 원시 라이다 후보: `(x=0.128, y=0.578)`, `r=0.592 m`
- 카메라 융합 결과: 없음

다른 초기 예시:

- `24.056 s`: 원시 후보 `(1.399, 1.475)`, `r=2.033 m`; 융합 후보 없음
- `27.347 s`: 원시 후보 `(0.704, 1.480)`, `r=1.639 m`와 `(2.093, -0.386)`, `r=2.128 m` 중 뒤쪽 후보만 융합됨

이는 카메라 연관 조건이 주변 구조물 또는 관계없는 점군을 제거하고 있음을 보여준다. 따라서 조기 감속을 추가하더라도 원시 라이다 후보만으로 조향 또는 강한 감속을 시작하지 않는 편이 안전하다.

## 7. YOLO 검출 통계

| 항목 | 값 |
|---|---:|
| 전체 객체 검출 프레임 | `129` |
| 라바콘이 검출된 프레임 | `58` |
| confidence 0.5 이상 라바콘 프레임 | `58` |
| 최소 confidence | `0.531` |
| 최대 confidence | `0.925` |
| 평균 confidence | `0.860` |

첫 검출 confidence도 임계값을 넘었고 이후 confidence가 충분히 상승했다. 이번 실험에서는 YOLO confidence 부족보다 경로 구성 조건이 실제 전환 시점을 결정했다.

## 8. 수동 이동 속도 참고값

최초 융합 후보가 나온 시점부터 실제 진입 판단까지 가장 가까운 후보의 방사거리 변화를 이용하면 다음과 같다.

```text
(2.176 m - 1.397 m) / 2.637 s = 약 0.300 m/s
```

이는 차량을 손으로 민 이번 실험 구간의 대략적인 접근 속도일 뿐이며, 자율주행 중 속도 25의 차량 동역학을 나타내는 값은 아니다.

전체 실험에서 채택 후보의 최근접 방사거리는 약 `0.329~2.176 m` 범위였다.

## 9. VESC 설정에 따른 속도 환산

현재 설정 파일:

```text
src/xycar_device/xycar_vesc_driver/config/xycar_vesc_driver.yaml
```

관련 설정값:

| 설정 | 값 |
|---|---:|
| `speed_to_mps_gain` | `0.08` |
| `speed_to_erpm_gain` | `4614` |
| `control_hz` | `20 Hz` |
| `telemetry_poll_hz` | `50 Hz` |
| 가속도 제한 | `0.4 m/s²` |
| 감속도 제한 | `1.5 m/s²` |

속도 명령은 다음과 같이 환산된다.

```text
속도[m/s] = 속도 명령 × 0.08
```

| 명령 | 목표 속도 | 목표 속도 | VESC 목표 eRPM |
|---:|---:|---:|---:|
| `25` | `2.00 m/s` | `7.20 km/h` | 약 `9,228` |
| `10` | `0.80 m/s` | `2.88 km/h` | 약 `3,691` |

드라이버는 받은 속도 명령을 즉시 목표값으로 설정하되, 실제 VESC에 적용하는 속도는 제어 주기마다 가감속 제한에 맞춰 선형적으로 변화시킨다.

## 10. 25에서 10까지의 이론 감속 계산

가정:

```text
초기 속도 v0 = 25 × 0.08 = 2.00 m/s
목표 속도 vt = 10 × 0.08 = 0.80 m/s
설정 감속도 a = 1.50 m/s²
```

필요 시간:

```text
t = (v0 - vt) / a
  = (2.00 - 0.80) / 1.50
  = 0.80 s
```

필요 거리:

```text
d = (v0² - vt²) / (2a)
  = (2.00² - 0.80²) / (2 × 1.50)
  = 1.12 m
```

방사거리 `1.397 m`만 사용하면 목표 속도 도달 후 약 `0.277 m`가 남는 것으로 보인다. 그러나 차량 진행 방향의 실제 여유는 전방거리 `1.320 m`이므로 지연을 무시해도 남는 거리는 약 `0.200 m`다.

## 11. 제어 지연을 포함한 계산

과거 2026-08-19 통합 주행 bag에서 `CONE_RULE` 상태가 발생한 시점부터 `/xycar_motor` 명령이 확인될 때까지 측정한 6회의 지연은 다음과 같다.

```text
13.1 ms, 10.7 ms, 12.5 ms, 13.4 ms, 12.5 ms, 2.6 ms
```

- 중앙값: `12.53 ms`
- 최대값: `13.44 ms`

여기에 최대 약 `50 ms`의 VESC 제어 주기를 고려하면 소프트웨어 계층의 합리적인 총 지연 범위는 약 `0.05~0.07 s`다. 속도 `2.0 m/s`에서 이 지연 동안 이동하는 거리는 약 `0.10~0.14 m`다.

따라서 전방거리 `1.320 m` 중 실제 감속에 남는 거리는 약 `1.18~1.22 m`이며, 이론상 필요한 `1.12 m`보다 불과 약 `0.06~0.10 m` 길다.

더 큰 총 지연을 가정한 보수적 계산:

| 총 지연 | 지연 중 이동 | 감속에 남는 거리 | 입구 속도 계산 | 명령 환산 |
|---:|---:|---:|---:|---:|
| `0.10 s` | `0.20 m` | `1.12 m` | `0.80 m/s` | `10.0` |
| `0.15 s` | `0.30 m` | `1.02 m` | `sqrt(4 - 2×1.5×1.02) = 0.97 m/s` | `12.1` |
| `0.20 s` | `0.40 m` | `0.92 m` | `sqrt(4 - 2×1.5×0.92) = 1.11 m/s` | `13.9` |

## 12. 과거 실제 VESC 데이터와의 비교

2026-08-19 통합 주행 bag에는 실제 VESC 속도 데이터가 존재한다. 당시에도 현재와 같은 속도 환산값과 감속도 설정이 사용됐다.

참고한 bag:

```text
/home/xytron/rosbags/0819 Kookmin/integrated_drive/8_19_thrid_drive_통합주행 지름길_장애물_콘_기타등등/8_19_second_drive_0.db3
/home/xytron/rosbags/0819 Kookmin/integrated_drive/8_19_fourth_drive_20260819_151303/8_19_fourth_drive_20260819_151303_0.db3
```

관측된 실제 감속 사례:

- 감속 전 명령: `25`
- 감속 전 실제 측정 속도: 약 `1.85 m/s`
- 목표 명령: `9`
- 도달한 측정 속도: 약 `0.75 m/s`
- 감속 소요 시간: 약 `0.676 s`
- 해당 시간 동안 적분한 이동거리: 약 `0.871 m`

이 데이터는 실제 차량이 설정 감속 제한에 따라 비교적 빠르게 감속했으며, 이번에 확보된 전방거리 `1.320 m`가 일반적인 조건에서는 `25 → 10` 감속에 충분할 가능성이 높다는 판단을 뒷받침한다.

단, 이는 정확히 같은 `25 → 10` 전환을 현재 코스에서 측정한 자료가 아니므로 최종 실차 검증을 대신할 수는 없다.

## 13. 진입 전 선택기 출력 분포

- `CONE_RULE` 전환 전 selector 상태에서 속도 `25`: `590`개
- `CONE_RULE` 전환 전 selector 상태에서 속도 `16`: `9`개
- 전환 직전 출력: 속도 `25`
- 전환 후 기록된 selector 상태: 모두 속도 `10` (`259`개)

따라서 현재 통합 로직은 라바콘이 멀리서 보인다는 이유만으로 미리 속도 10으로 낮추지 않는다. 유효 라바콘 경로가 확정되어 `CONE_RULE`로 들어가는 순간 속도를 10으로 전환한다.

## 14. 권장 수정 방향

현재 조향 진입 조건은 주변 가구나 구조물을 라바콘으로 오인하지 않기 위해 유지하는 것이 좋다. 대신 조향권 전환과 별개인 **속도 전용 사전 감속 단계**를 두는 방식을 권장한다.

권장 예시:

1. YOLO 라바콘 검출이 2회 이상 연속 확인되고, 카메라 융합 라바콘 후보가 2회 이상 유지되며 최근접 거리가 약 `2.2 m` 이내이면 속도 `25 → 15`로 사전 감속한다.
2. 융합 후보가 2개 이상이거나 최근접 거리가 `1.8 m` 이내이면 속도 `15 → 10`으로 낮춘다.
3. 실제 조향 제어권은 현재와 동일하게 유효 라바콘 경로 명령 3회 연속 확인 후 `CONE_RULE`로 넘긴다.
4. 원시 라이다 후보만으로는 사전 감속이나 조향을 시작하지 않고, YOLO와 카메라-라이다 융합 조건을 함께 사용한다.

이 방식은 조향 오인식을 막는 현재 경로 검증 조건을 유지하면서, 라바콘 입구에서의 감속 여유를 늘릴 수 있다.

## 15. 추가 실차 검증에 필요한 토픽

현재 수동 실험 bag에는 실제 차량 속도가 없으므로, 현재 차량으로 `25 → 10` 전환을 정확하게 검증하려면 다음 토픽을 함께 기록해야 한다.

```text
/xycar_motor
/vehicle/vesc_state
/xycar_motor_bridge/debug
/odom
/hybrid_gate/status
/hybrid_gate/diagnostics
/my_rule/cone_cmd
/my_rule/cone_status
/my_rule/cone_diagnostics
/my_rule/cone_fused_clusters
/my_rule/object_detections
/scan
```

실차 검증 시 확인할 핵심 값:

- `CONE_RULE` 전환 순간의 전방거리 `x`
- 전환 직전 실제 VESC 속도
- 속도 명령 10이 브리지와 VESC에 적용된 시각
- 실제 속도가 `0.8 m/s` 부근에 도달한 시각과 이동거리
- 라바콘 입구 기준선을 통과할 때의 실제 속도

## 16. 분석의 한계

- 이번 bag은 손으로 차량을 민 실험이므로 실제 모터와 차량 관성 데이터가 없다.
- 라바콘까지의 `x = 1.320 m`는 가장 가까운 채택 라바콘의 전방 좌표이며, 실제 두 경계 사이에 정의한 입구 기준선까지의 정확한 거리는 아니다.
- VESC의 실제 감속은 배터리 전압, 노면 마찰, 타이어 상태, 차량 하중, 모터 및 VESC의 폐루프 제동 특성에 따라 달라진다.
- 계산은 속도 명령 25로 정상 주행 중이었다는 가정과 설정 감속도 `1.5 m/s²`가 실제로 구현된다는 가정에 기반한다.

따라서 이 문서의 수치는 현재 코드와 확보된 데이터에 기반한 합리적인 예상값이며, 최종 안전 판단은 실제 주행 rosbag의 VESC 속도 데이터로 검증해야 한다.
